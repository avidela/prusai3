[1mdiff --git a/Marlin/Configuration.h b/Marlin/Configuration.h[m
[1mindex 517d30d..865c110 100644[m
[1m--- a/Marlin/Configuration.h[m
[1m+++ b/Marlin/Configuration.h[m
[36m@@ -332,13 +332,13 @@[m [mHere are some standard links for getting your machine calibrated:[m
 #endif[m
 [m
 // Mechanical endstop with COM to ground and NC to Signal uses "false" here (most common setup).[m
[31m-const bool X_MIN_ENDSTOP_INVERTING = true; // set to true to invert the logic of the endstop.[m
[31m-const bool Y_MIN_ENDSTOP_INVERTING = true; // set to true to invert the logic of the endstop.[m
[31m-const bool Z_MIN_ENDSTOP_INVERTING = true; // set to true to invert the logic of the endstop.[m
[31m-const bool X_MAX_ENDSTOP_INVERTING = true; // set to true to invert the logic of the endstop.[m
[31m-const bool Y_MAX_ENDSTOP_INVERTING = true; // set to true to invert the logic of the endstop.[m
[31m-const bool Z_MAX_ENDSTOP_INVERTING = true; // set to true to invert the logic of the endstop.[m
[31m-const bool Z_MIN_PROBE_ENDSTOP_INVERTING = true; // set to true to invert the logic of the endstop.[m
[32m+[m[32mconst bool X_MIN_ENDSTOP_INVERTING = false; // set to true to invert the logic of the endstop.[m
[32m+[m[32mconst bool Y_MIN_ENDSTOP_INVERTING = false; // set to true to invert the logic of the endstop.[m
[32m+[m[32mconst bool Z_MIN_ENDSTOP_INVERTING = false; // set to true to invert the logic of the endstop.[m
[32m+[m[32mconst bool X_MAX_ENDSTOP_INVERTING = false; // set to true to invert the logic of the endstop.[m
[32m+[m[32mconst bool Y_MAX_ENDSTOP_INVERTING = false; // set to true to invert the logic of the endstop.[m
[32m+[m[32mconst bool Z_MAX_ENDSTOP_INVERTING = false; // set to true to invert the logic of the endstop.[m
[32m+[m[32mconst bool Z_MIN_PROBE_ENDSTOP_INVERTING = false; // set to true to invert the logic of the endstop.[m
 #define DISABLE_MAX_ENDSTOPS[m
 //#define DISABLE_MIN_ENDSTOPS[m
 [m
[36m@@ -369,9 +369,9 @@[m [mconst bool Z_MIN_PROBE_ENDSTOP_INVERTING = true; // set to true to invert the lo[m
 // @section machine[m
 [m
 // Invert the stepper direction. Change (or reverse the motor connector) if an axis goes the wrong way.[m
[31m-#define INVERT_X_DIR true[m
[31m-#define INVERT_Y_DIR true[m
[31m-#define INVERT_Z_DIR true[m
[32m+[m[32m#define INVERT_X_DIR true[m[41m [m
[32m+[m[32m#define INVERT_Y_DIR true[m[41m  [m
[32m+[m[32m#define INVERT_Z_DIR true[m[41m [m
 [m
 // @section extruder[m
 [m
[36m@@ -390,8 +390,8 @@[m [mconst bool Z_MIN_PROBE_ENDSTOP_INVERTING = true; // set to true to invert the lo[m
 #define Y_HOME_DIR -1[m
 #define Z_HOME_DIR 1[m
 [m
[31m-#define min_software_endstops true // If true, axis won't move to coordinates less than HOME_POS.[m
[31m-#define max_software_endstops true  // If true, axis won't move to coordinates greater than the defined lengths below.[m
[32m+[m[32m#define min_software_endstops false // If true, axis won't move to coordinates less than HOME_POS.[m
[32m+[m[32m#define max_software_endstops false  // If true, axis won't move to coordinates greater than the defined lengths below.[m
 [m
 // @section machine[m
 [m
warning: LF will be replaced by CRLF in Marlin/Configuration.h.
The file will have its original line endings in your working directory.
